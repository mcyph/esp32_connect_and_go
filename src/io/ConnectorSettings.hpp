/*typedef struct SPISettings {
    int dataInPin;
    int dataOutPin;
    int clockPin;
    DefaultHighPin chipSelectPin;  // on analog multiplexer
    DefaultLowPin resetPin;        // on analog multiplexer
    DefaultLowPin analogInPin;     // on digital multiplexer
    DefaultLowPin extraOutPin2;    // on digital multiplexer
};
*/
